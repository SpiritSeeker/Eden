#pragma once

#include "Eden/Core.h"

namespace Eden {

	class EDEN_API Player
	{
	public:
		Player();
		~Player();
		
		// std::string GetDeviceName(int index);
		// void SetDevice(std::string name);
	};

}